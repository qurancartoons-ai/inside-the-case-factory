[project]
name = "inside-the-case-factory"
version = "0.1.0"
description = "Offline-first scaffold for an automated cinematic documentary video production pipeline."
requires-python = ">=3.12"
dependencies = []

[project.scripts]
case-factory = "inside_case_factory.cli.main:main"

